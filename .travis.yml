language: python
python:
        - "3.5"
install:
        - pip install -r requirements.txt
        - pip install codecov
script:
        - pytest --cov-report term-missing --cov=app
        - coveralls
after_success:
        - coveralls
        - codecov --token
        - codeclimate-test-reporter
